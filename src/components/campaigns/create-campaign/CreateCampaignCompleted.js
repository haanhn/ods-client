import React from 'react';

const CreateCampaignCompleted = (props) => {
    return (
        <div style={{fontSize: '300%', textAlign: 'center', height: '300px'}}>
            {props.message}
        </div>
    );
}

export default CreateCampaignCompleted;